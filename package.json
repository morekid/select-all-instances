{
  "name": "select-all-instances",
  "version": "1.0.0",
  "description": "A plugin to inspect and work with component instance selection.",
  "main": "code.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "npm run build -- --watch"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "^1.69.0",
    "typescript": "*"
  }
}
